# PROBLEMS — needs-fixing list

> Audited 2026-09-22 against `AGENTS.md` + `docs/{Performance,SEO,Accessibility,BestPractices}.md`.
> Resolved same day — see each item's fix. **Gate now GREEN**: `typecheck` ✅ · `lint` ✅ (0 warnings) ·
> `format:check` ✅ · `test` 12/12 ✅ · Chromium headless (Playwright 1243) zero console errors, hero renders.
> Nothing committed — you decide push/deploy.

---

## P0 — gate / correctness ✅ ALL FIXED

1. ✅ **Prettier drift** — ran `npm run format`; `format:check` passes. (AGENTS.md §17)
2. ✅ **`console.log` in TopBar** — debug log deleted (`TopBar.tsx`). Lint warning gone. (AGENTS.md §3.8)
3. ✅ **GSAP registration unified** — `Observer` moved into `App.tsx:14` (`ScrollTrigger, SplitText, Observer`); per-section `registerPlugin` calls deleted from `About`/`Hero`/`Contact`. (`docs/ARCHITECTURE.md` §1)
4. ✅ **Footer morph link** — external v1 link kept as `<a>` (different app, correct) + `rel="noopener noreferrer"` added (`Footer.tsx`). (SEO §2.2, BestPractices §8)
5. ✅ **Duplicate `.row-img-1` targets** — rows now `row-img-1`/`row-img-2`, each animated with its own row (`About.tsx`). (correctness)
6. ✅ **`useGSAP` scoped** — `rootRef` + `ref` + `{ scope: rootRef }` in About/Hero/Projects/Experience/Contact/Footer (+ Footer morph hook); section-level queries use `root.querySelector`. Tools already had it. (`docs/ARCHITECTURE.md` §2)
7. ✅ **`invalidateOnRefresh` on pins** — added to About/Experience/Projects/Tools scrub timelines. (Contact/Footer have no scrub pins.) (`docs/ARCHITECTURE.md` §2)

---

## P1 — performance / SEO / a11y ✅ ALL FIXED

8. ✅ **`index.html` meta** — canonical + `og:url` → `https://saifayman23.github.io/portfolio/v2/`; `og:image`/`twitter:image` → `/portfolio/v2/og.webp` (1200×630); `twitter:card` → `summary_large_image`; JSON-LD urls/logo fixed. (AGENTS.md §10, SEO §3/§4)
9. ✅ **OG image** — new `public/og.webp` (1200×630, 10 KB, generated by `scripts/og-image.mjs`); `getOgImage()` returns it. (SEO §4)
10. ✅ **Empty `id={''}`** — prop removed from Hero `JapaneseText`. (BestPractices §1)
11. ✅ **Single `<h1>`** — Hero `h1#hero-title` is now the only `h1`; About/Experience/Projects/Contact/Footer titles demoted to `h2` (+`font-display` to preserve look); decorative duplicates → `div aria-hidden`. (SEO §6, Accessibility §1.4)
12. ✅ **Decorative alt** — Footer bg `alt=""`. (Accessibility §4.1)
13. ✅ **Lazy below-fold images** — Experience covers + Footer bg got `loading="lazy"` + `decoding="async"`; all `CyberImage` call sites already lazy by default. (Performance §4)
14. ✅ **AboutRow orphan deleted** (`git rm`). (SPEC §5, AGENTS.md §3.6)
15. ✅ **Jet canvas pauses** — `visibilitychange` freezes `frameloop` (`JetScene.tsx`); **WebGL gate added** (`lib/webgl.ts`): no-GL devices skip `Canvas`/`PixelBlast` instead of white-screening (found live in Chromium: uncaught `Error creating WebGL context` unmounted the tree). (Performance §3.5, BestPractices §1)
16. ✅ **Heavy libs split** — `JetScene` + `PixelBlast` are `lazy()` chunks now (`JetScene-*.js` 238 KB, `PixelBlast-*.js` 54 KB out of the critical path). (Performance §3, AGENTS.md §5)
17. ✅ **Motion toggle documented** — JSDoc on `lib/motion.ts:reduceMotion` (reload needed after timelines exist). (Accessibility §2)
18. ✅ **Subpath SEO** — `SITE_URL` default + sitemap/robots emit under `/portfolio/v2`; deploy env may still override via `VITE_SITE_URL`. (SEO §1/§3)

---

## P2 — hygiene / coverage ✅ ALL FIXED

19. ✅ **Section axe coverage** — new `src/test/a11y-sections.test.tsx` (7 scans, all green) + `matchMedia` stub in `test/setup.ts`. Scans caught and fixed 4 serious issue groups (below). (BestPractices §6, AGENTS.md §14)
20. ✅ **`public/models` pruned** — 5 unreferenced screenshots (~3.7 MB, `git rm`); `jet.glb` kept. (Performance §3)

---

## Found by the new axe scans (all fixed)

- **SplitText `aria-label` on generic elements** (`aria-prohibited-attr`, serious) — added `aria: 'none'` to splits on div/p/span (Hero intro/blurbs/japanese/subtitle, About rows, Contact `p`, Projects descriptions, Footer japanese); headings keep default labeling. (Accessibility §7, gsap-plugins skill)
- **Nested interactive in HeroInfo** (`nested-interactive`, serious) — `Button` gained `href`/`target`/`rel` support rendering a real `<a>` root, spacer copy marked `inert`; `HeroInfo` no longer nests `<a>` in `<button>`. (Accessibility §7)
- **Radar icon SVG without label** (`svg-img-alt`, serious) — `data-slot="radar-icon"` container is `aria-hidden` (decorative; name already text). (Accessibility §4.2)
- **Missing `Observer` import in Contact** — registration moved to `App.tsx` but usage stayed; would have thrown in production. Re-added import. (correctness)

---

## Chromium smoke test (Playwright chromium-1243, `vite preview`, 1728×1000)

- Before fixes: blank white page + uncaught WebGL error (no-GPU headless), plus startup warnings.
- After fixes: hero renders fully (TopBar, title, blurbs, jet, PixelBlast dots) with **zero console errors/warnings**.
- Metrics note: lab numbers need a throttled mobile run (Lighthouse CI); field data comes from CrUX after deploy. Structure is now compliant: lazy chunks, lazy images, `font-display: swap`, reserved image space, GPU-only transforms.

---

## Verified good (do not regress)

- `vite.config.ts` `base: '/portfolio/v2/'` + Router `basename` + `%BASE_URL%` icons in `dist`; model URL `/portfolio/v2/models/jet.glb` in bundle; `v2/` tree on `origin/gh-pages`.
- Paragraph `SplitText` wraps on words (`type: 'words, chars'`, chars animated); Hero blurbs use masked `lines`.
- Skills-panel uses `fromTo` + `overwrite: 'auto'`; all splits `revert()` on cleanup.
- Smart tracker: per-section progress 0–100, `active` via `isActive`.
